# 小组项目提案（数据验收后修订稿）

## 项目名称
**Steam 游戏爆款预测：发行商如何用"发售前可得的信息"判断游戏能否成为爆款**

- 课程：IBA6102 Machine Learning for Business
- 状态：题目已锁定；数据验收 **Conditional Go**

---

## 1. 商业问题（Business Problem）

一家中小型游戏发行商资金有限，每年只能发行/代理少数几款游戏。它需要**在立项/发行决策时**判断：

> 「这款游戏上架后，会不会成为爆款？」

据此决定把预算投向哪类游戏（类型、平台、定价、目标年龄层等）。这是一个真实的商业决策问题——**发行预算分配**，而不是单纯的"事后统计哪个游戏火"。

## 2. ML Task（T-P-E 框架）

- **T（任务）**：二分类与候选项目排序
  - 输入（Input）：相对稳定、在发行决策时原则上可获得的产品属性（见 §4）
  - 输出（Output）：`hit`（同发行年份市场热度前 10%）/ `not hit`，以及候选项目得分排序
- **P（性能）**：类别不平衡（爆款少），因此不只看 accuracy
  - 主指标：Average Precision / PR-AUC、ROC-AUC、Top 10% precision / recall / lift
  - 商业口径：发行商只能深入评估少数候选项目，因此重点看固定筛选比例下的命中率和提升倍数
- **E（经验/数据）**：Kaggle「Steam Store Games (Clean dataset)」，约 27,000 款游戏，2019 快照

## 3. 为什么这个题成立（数据对齐论证）

关键设计纪律：**不使用发行后结果字段预测结果；所有验证按时间向前推进**。该数据是 2019 年单次快照，不能证明所有字段都是各游戏发售当天的历史值，因此本项目定位为回顾性的发行筛选原型，不作因果推断。

1. **循环泄漏**：若用 `owners`/好评数预测"是否爆款"，等于用结果预测结果 → 剔除。
2. **累积效应（age confounder）**：`owners`、评价数是从上架累积到 2019 的，老游戏天然数值大。标签必须在同一发行年份内定义，且 2019 年不完整样本必须排除。

### 最终主标签（y）

`total_ratings = positive_ratings + negative_ratings`。在每个 `release_year` 内，`total_ratings` 达到该年份第 90 百分位则 `hit = 1`，否则为 0。该标签表示**高市场热度/用户参与度**，不等于销量、收入、利润或产品质量。

数据验收发现，SteamSpy `owners` 只有 13 个区间；直接用年份内第 90 百分位会因大量并列把整体正类推高至 20.97%，2019 年甚至达到 100%。因此 `owners` 只作次要的有序稳健性检验。好评率只作质量侧辅助分析，并设置最小评价数门槛。

### 数据验收结果

六张 CSV 已取得并通过哈希、ZIP、字段、主键、重复、缺失、日期、标签可构造性和表间覆盖检查。主表为 27,075 × 18；建模使用 2018 年及以前的 24,862 款游戏。完整证据见 `docs/DATA_AUDIT.md`。

## 4. 特征工程方向与时间戳边界

- 类别展开：`genres` / `categories` 拆多列（one-hot / multi-hot）
- 平台：Windows / Mac / Linux 支持数
- `required_age` 年龄分级
- 发行月份/季度（季节性）；`release_year` 主要用于构造标签和时间切分
- 厂商：`developer` / `publisher`，所有历史次数、命中率或信誉编码只使用此前发行记录
- 语言：是否支持英文 `english`
- 敏感性模型才加入：快照价格、成就数量、描述/配置/媒体信息

### 禁用特征（泄漏红线，写入报告）
`positive_ratings`、`negative_ratings`、`owners`、`average_playtime`、`median_playtime`、`steamspy_tags`、全部社区 tag 投票数、评价数相关聚合 → 一律不进入 X。

其中 SteamSpy tags 来自玩家投票，是发行后的社区反馈，不是发售前特征。

## 5. 建模与评估计划

1. 预处理：multi-hot / one-hot / 标准化，全部只在历史训练数据上 fit。
2. 规则基线：此前发行记录计算的 developer/publisher 平滑命中率。
3. **Logistic Regression**：可解释的 ML 基线。
4. 一个正则化树集成模型：优先 XGBoost；环境不允许时使用 Random Forest。
5. 时间验证：训练集为 2016 年及以前，验证集为 2017 年，最终测试集为 2018 年；禁止随机切分和 shuffled CV。
6. 指标：Average Precision / PR-AUC、ROC-AUC、Top 10% precision / recall / lift；阈值只用验证集选择。
7. 可解释：系数、feature importance 或 SHAP，总结候选项目画像。
8. 交付：给发行商的 3–5 条筛选建议，并明确非因果、非 ROI 边界。

## 6. 预期产出与"给管理者的建议"示例

- 结论形态：在固定 10% 深度评估容量下，模型筛选组合的命中率、召回率和相对随机筛选的 lift。
- 附：规则基线、Logistic Regression 和树模型的样本外比较表。
- 局限声明：数据为 2019 快照、部分属性不是历史发售值、SteamSpy owners 为估算；方法与结论需用更新的时间戳数据复验。

## 7. 风险与缓解
| 风险 | 缓解 |
|---|---|
| 标签受累积效应污染 | 同一发行年份内定义热度前 10%，并排除不完整的 2019 |
| owners 区间过粗、估算误差大 | 只作有序稳健性分析，不作主标签 |
| 类别不平衡导致 accuracy 虚高 | 以 PR-AUC、ROC-AUC 和 Lift@10% 为主 |
| 快照属性不等于发售时属性 | 主模型用相对稳定字段；价格等只作敏感性分析 |
| 冷启动厂商占比高 | 加入 genre/category/platform 等产品特征，并单报 unseen 厂商表现 |

## 8. 时间线（对齐课程）
| 周次 | 里程碑 |
|---|---|
| W1 | 组队、确定本题目 |
| W2–4 | 数据验收、字段核对、锁定标签与时间切分 |
| W5–6 | 完成 EDA、规则基线和 Logistic Regression |
| W7–12 | 特征工程 → 建模比较 → SHAP 解读 → 结论打磨 |
| W13–14 | 展示 |
| W15 | 提交报告 |

## 9. 团队角色分工与任务清单（7–8 人）

| 角色 | 人数 | 具体任务 | 产出 |
|---|---|---|---|
| 业务与问题定义 | 1–2 | 明确筛选决策、使用者、热度代理与非因果边界；写故事线 | 把结果翻译成《给发行商的 3–5 条建议》 |
| 数据与 EDA | 1–2 | 按已锁定口径构造标签、时间切分、缺失/异常和冷启动分析 | 数据说明文档 + EDA notebook + 图表 |
| 特征工程 | 1–2 | `genres`/`categories` multi-hot、平台、季节性、时间安全厂商历史编码 | 只含允许字段的最终 X |
| 建模与算法 | 2 | 规则基线 → Logistic Regression → 一个树集成模型；2017 调优、2018 一次测试 | 三类方法的样本外对比表 |
| 评估与可解释性 | 1–2（建模兼） | PR-AUC、ROC-AUC、Top 10% precision/recall/lift；系数或 SHAP | 评估报告 + 特征重要性图 + 候选项目画像 |
| 报告与展示 | 1–2 | 报告 + PPT，讲清"商业问题→任务→数据→模型→建议"闭环；推进度 | 展示 slides + 15 周报告 + 协作记录 |

> 建议分配：业务/汇报 2、数据 EDA 1–2、特征 1–2、建模 2、可解释 1（建模兼）。注意展示占 10 分，故事讲得清楚比堆模型更关键，务必保留会讲的人。

---
*本文件已同步 2026-09-12 数据验收结论，仍会随建模结果更新。*
