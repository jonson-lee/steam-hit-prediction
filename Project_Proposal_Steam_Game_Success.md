# 小组项目提案（初稿）

## 项目名称
**Steam 游戏爆款预测：发行商如何用"发售前可得的信息"判断游戏能否成为爆款**

- 课程：IBA6102 Machine Learning for Business
- 状态：初稿（题目暂定）
- 关联文件：`08_Study_Notes/ML_Knowledge_Map.md`（方法论参考）

---

## 1. 商业问题（Business Problem）

一家中小型游戏发行商资金有限，每年只能发行/代理少数几款游戏。它需要**在立项/发行决策时**判断：

> 「这款游戏上架后，会不会成为爆款？」

据此决定把预算投向哪类游戏（类型、平台、定价、目标年龄层等）。这是一个真实的商业决策问题——**发行预算分配**，而不是单纯的"事后统计哪个游戏火"。

## 2. ML Task（T-P-E 框架）

- **T（任务）**：二分类
  - 输入（Input）：发售时即可得的产品属性（A 类特征，见 §4）
  - 输出（Output）：`hit`（爆款） / `not hit`（非爆款）
- **P（性能）**：类别不平衡（爆款少），因此不只看 accuracy
  - 主指标：AUC、precision / recall、confusion matrix
  - 商业口径：投错项目＝预算亏损 → precision 重要；错过爆款＝机会成本 → recall 重要；最终以"决策阈值"讲 trade-off
- **E（经验/数据）**：Kaggle「Steam Store Games (Clean dataset)」，约 27,000 款游戏，2019 快照

## 3. 为什么这个题成立（数据对齐论证）

关键设计纪律：**问题只使用"发售时可得的 A 类属性"预测，不使用"发行后结果 B 类字段"当特征**，从而避免两大数据陷阱：

1. **循环泄漏**：若用 `owners`/好评数预测"是否爆款"，等于用结果预测结果 → 剔除。
2. **累积效应（age confounder）**：`owners`、评价数是从上架累积到 2019 的，老游戏天然数值大。若用累计数定义爆款，年份与"是否爆款"混淆 → 用「好评率」或「同年代分组内排名」定义标签来规避。

### 标签（y）定义 —— 二选一（建议都试）
- **方案 A（质量型）**：`好评率 = positive/(positive+negative)` ≥ 阈值（如 0.85）→ 1，否则 0。好评率相对不受游戏年龄影响。
- **方案 B（商业型）**：按 `release_year` 分组，组内按 `owners`（取区间中点）排名前 10% → 1。同龄游戏相比才公平。

### 可用的候选数据集（待下载确认字段）
| 数据集 | 规模/时间 | 说明 |
|---|---|---|
| Steam Store Games (Clean dataset)（主选） | ~27,000 款，2019 | 免费，CC BY 4.0，Steam + SteamSpy 合并清洗 |
| Steam Games Dataset（备选） | ~80,000 款，较新 | 字段更全更近，需下载后核对手头有无 A 类字段 |

> 行动项：下载后**第一时间核对列名**，按 §4 把特征分为 A / B 两类并写进 README。

## 4. 特征工程方向（只保留 A 类 = 发售时可得）

- 类别展开：`genres` / `categories` 拆多列（one-hot / 频次编码）
- 平台：Windows / Mac / Linux 支持数
- 价格：是否免费、价格分箱（免费与低价游戏爆款路径不同）
- `required_age` 年龄分级
- `achievements` 成就数量（可作开发投入的代理信号）
- 发行时间：`release_year`、月份（季节性）
- 厂商：`developer` / `publisher`（构造"是否大厂/老厂"标志，控制长尾）
- 语言：是否支持英文 `english`
- 缺失值处理 + `owners` 区间转中点（仅用于标签 B）

### 禁用特征（泄漏红线，写入报告）
`positive_ratings`、`negative_ratings`、`owners`（作特征时）、`average_playtime`、`median_playtime`、`metacritic`、`ccu`、评价数相关聚合 → 一律不进入 X。

## 5. 建模与评估计划

1. 预处理：one-hot / 标准化（`StandardScaler` 只在训练集 fit）
2. 基线：**Logistic Regression**（可解释、给下限）
3. **Decision Tree**（看剪枝/过拟合）
4. **Random Forest**（基线强模型）
5. **XGBoost / LightGBM**（表格数据主力，预期最优）
6. 评估纪律：train/valid/test 或 5 折交叉验证，**样本外比较**
   - 指标：AUC、precision/recall、F1、confusion matrix；不平衡用 class_weight 或调阈值
7. 可解释：feature importance / SHAP → 总结「爆款画像」
8. 交付：给发行商的 3–5 条可执行建议（见 §6）

## 6. 预期产出与"给管理者的建议"示例

- 结论形态示例：
  > 「在 2015–2019 年发行样本中，动作类 + 多人 + 英文支持 + 中等定价 的项目命中爆款率 X%，显著高于均值 Y%。建议预算优先投向该类项目；同时应回避 Z 类低命中类型。」
- 附：模型在关键阈值下的 precision/recall 权衡表（投 n 个项目命中 m 个的期望）
- 局限声明：数据为 2019 快照、价格等属性为现值非当年发行值、SteamSpy `owners` 为估算；方法与结论需用更新数据复验。

## 7. 风险与缓解
| 风险 | 缓解 |
|---|---|
| 标签定义受累积效应污染 | 用好评率 / 同年代分组排名；报告里说明 |
| 类别不平衡导致 accuracy 虚高 | 只看 AUC/precision/recall，不报裸 accuracy |
| 老游戏属性（价格等）与当年不符 | 报告诚实说明；可限样本 2015–2019 |
| Kaggle 下载需账号 | 团队成员各自注册；必要时换备选数据集 |
| 缺字段/缺目标分布 | 先做 EDA 再定最终标签阈值 |

## 8. 时间线（对齐课程）
| 周次 | 里程碑 |
|---|---|
| W1 | 组队、确定本题目 |
| W2–4 | 下载数据、核对字段、EDA、敲定标签定义 |
| W5–6 | 写提案（本章即初稿）、定基线 |
| W7–12 | 特征工程 → 建模比较 → SHAP 解读 → 结论打磨 |
| W13–14 | 展示 |
| W15 | 提交报告 |

## 9. 团队角色分工与任务清单（7–8 人）

| 角色 | 人数 | 具体任务 | 产出 |
|---|---|---|---|
| 业务与问题定义 | 1–2 | 敲定"爆款"商业口径（好评率阈值 vs 同年代 owners 前 10%）；明确给发行商的决策与使用方；写问题背景与结论故事线 | 把结果翻译成《给发行商的 3–5 条建议》 |
| 数据与 EDA | 1–2 | `owners` 区间转数字、缺失/异常处理、类别不平衡摸底；核对 A/B 字段切分与泄漏红线；分布/相关性/目标分布可视化 | 数据说明文档 + EDA notebook + 图表 |
| 特征工程 | 1–2 | `genres`/`categories` one-hot、价格/免费分箱、平台数、`release_year`、厂商标志、`achievements`；可选合并 tag 表（防特征爆炸） | 最终 X 特征矩阵（只含 A 类字段） |
| 建模与算法 | 2 | 基线逻辑回归 → 决策树 → 随机森林 → XGBoost；5 折交叉验证、类别不平衡（class_weight/阈值） | 四模型样本外对比表 |
| 评估与可解释性 | 1–2（建模兼） | 统一 AUC/precision/recall/F1 + confusion matrix；SHAP/feature importance | 评估报告 + 特征重要性图 + 爆款画像 |
| 报告与展示 | 1–2 | 报告 + PPT，讲清"商业问题→任务→数据→模型→建议"闭环；推进度 | 展示 slides + 15 周报告 + 协作记录 |

> 建议分配：业务/汇报 2、数据 EDA 1–2、特征 1–2、建模 2、可解释 1（建模兼）。注意展示占 10 分，故事讲得清楚比堆模型更关键，务必保留会讲的人。

---
*本文件为工作草稿，随课程进展更新。*
